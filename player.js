let playerCol=5,playerRow=5,targetCol=5,targetRow=5,movingUp=!1,movingDown=!1,movingLeft=!1,movingRight=!1,isSkating=!1,lastDir=0;function createGrid(){cols=Math.floor(innerWidth/tileSize),rows=Math.floor(innerHeight/tileSize),gameCanvas.style.width=`${cols*tileSize}px`,gameCanvas.style.height=`${rows*tileSize}px`;for(let e=0;e<cols*rows;e++){let t=document.createElement('div');t.className='tile',gameCanvas.appendChild(t)}gameCanvas.appendChild(skateboard),gameCanvas.appendChild(playerSvg)}function updatePlayerPosition(){playerSvg.style.left=`${playerCol*tileSize}px`,playerSvg.style.top=`${playerRow*tileSize}px`}setInterval(()=>{},beatDuration);